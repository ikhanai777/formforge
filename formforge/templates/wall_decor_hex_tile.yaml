id: wall_decor_hex_tile
version: 1
category: wall_decor
display_name: "Hex Tile Panel"
description: >
  Honeycomb wall panel: a hexagonal tile with a raised border and an optional
  perforated lattice centre. Tiles tessellate edge to edge, so a set builds a
  continuous honeycomb across a wall. Mounts with adhesive pads or a screw hole.
tags: [wall_decor, hexagon, honeycomb, panel, tile, modular, lattice, no-supports]
embedding_text: >
  hex tile honeycomb wall panel hexagon wall art modular wall tiles geometric
  wall decor hexagonal panel accent wall tiles
language: build123d
print_test:
  # No model in this repository has been physically printed. The target
  # machine and the rationale below are design intent, not a test record.
  status: untested
  target_printer: "Bambu A1"
  target_material: PLA
  rationale: >
    Tiles tessellate at exactly the across-flats dimension, so a set butts edge
    to edge with no gap. The 2 mm minimum on lattice ribs is about the
    unsupported spans between holes: thinner ribs are both weak and, on the
    first layer above the border, bridging further than they can hold.

expected_solids: 1

param_schema:
  type: object
  required: [across_flats_mm]
  properties:
    across_flats_mm:
      type: number
      minimum: 40
      maximum: 170
      default: 100
      description: "Flat to flat. Tiles tessellate at exactly this pitch."
      ui: { widget: slider, step: 5, label: "Across flats" }
    panel_t_mm:
      type: number
      minimum: 2
      maximum: 10
      default: 3
      ui: { widget: slider, step: 0.5, label: "Panel thickness" }
    border_w_mm:
      type: number
      minimum: 6
      maximum: 25
      default: 10
      description: "Must exceed the hanging hole by 4 mm so material remains either side."
      ui: { widget: slider, step: 1, label: "Border width" }
    border_h_mm:
      type: number
      minimum: 0
      maximum: 12
      default: 3
      description: "How far the border stands proud of the panel face."
      ui: { widget: slider, step: 0.5, label: "Border relief" }
    infill:
      type: string
      enum: [solid, hex_lattice, open]
      default: hex_lattice
      description: "'open' leaves just the border ring, for layering over another tile."
      ui: { widget: select, label: "Centre" }
    lattice_cell_mm:
      type: number
      minimum: 8
      maximum: 40
      default: 16
      description: "Lattice hole size across flats."
      ui: { widget: slider, step: 2, label: "Lattice cell" }
    lattice_rib_mm:
      type: number
      minimum: 2.0
      maximum: 8
      default: 3.0
      description: "Material between lattice holes. Under 2 mm the ribs droop."
    hang_d_mm:
      type: number
      minimum: 0
      maximum: 10
      default: 5
      description: "Hanging hole in the border. 0 for adhesive mounting only."

invariants:
  - "solids == 1"
  - "is_watertight"
  - "min_wall >= 1.5"
  - "mass_g <= 400"

preconditions:
  - "border_w_mm * 2 + 20 < across_flats_mm"
  - "infill == 'hex_lattice' implies lattice_cell_mm + lattice_rib_mm * 2 < across_flats_mm - border_w_mm * 2"
  - "hang_d_mm == 0 or hang_d_mm + 4 <= border_w_mm"

source: |
  from build123d import *
  import math

  ACROSS_FLATS_MM = 100.0
  PANEL_T_MM = 3.0
  BORDER_W_MM = 8.0
  BORDER_H_MM = 3.0
  INFILL = "hex_lattice"
  LATTICE_CELL_MM = 16.0
  LATTICE_RIB_MM = 3.0
  HANG_D_MM = 5.0

  APOTHEM_MM = ACROSS_FLATS_MM / 2
  INNER_APOTHEM_MM = APOTHEM_MM - BORDER_W_MM

  with BuildPart() as tile:
      with BuildSketch() as outline:
          RegularPolygon(APOTHEM_MM, 6, major_radius=False)
      extrude(amount=PANEL_T_MM)

      # Raised border ring, added on the face so the back stays flat against
      # the wall and flat on the build plate.
      if BORDER_H_MM > 0:
          with BuildSketch(Plane.XY.offset(PANEL_T_MM)) as border:
              RegularPolygon(APOTHEM_MM, 6, major_radius=False)
              RegularPolygon(INNER_APOTHEM_MM, 6, major_radius=False, mode=Mode.SUBTRACT)
          extrude(amount=BORDER_H_MM)

      if INFILL == "open":
          with BuildSketch(Plane.XY) as opening:
              RegularPolygon(INNER_APOTHEM_MM, 6, major_radius=False)
          extrude(amount=PANEL_T_MM, mode=Mode.SUBTRACT)

      elif INFILL == "hex_lattice":
          # A hexagon does not have the same reach in every direction, and the
          # lattice pitch has to respect that. RegularPolygon puts a vertex on
          # +X, so across the flats (Y) the cell spans 2 * apothem, while across
          # the corners (X) it spans 2 * circumradius -- about 15% more. Spacing
          # both axes by cell + rib overlaps neighbouring cells along X, which
          # eats the ribs between them and cuts the panel into loose pieces.
          cell_apothem = LATTICE_CELL_MM / 2
          # Flat-to-flat direction: the pitch is simply cell plus rib.
          pitch_y = LATTICE_CELL_MM + LATTICE_RIB_MM
          # Corner direction: three quarters of the corner-to-corner span, which
          # works out as the same pitch scaled by sqrt(3)/2.
          pitch_x = pitch_y * math.sqrt(3.0) / 2.0
          rows = int(INNER_APOTHEM_MM * 2 / pitch_y) + 2
          cols = int(INNER_APOTHEM_MM * 2 / pitch_x) + 2

          holes = []
          for col in range(-cols, cols + 1):
              for row in range(-rows, rows + 1):
                  x = col * pitch_x
                  # Alternate columns drop by half a pitch; that offset is what
                  # makes it a honeycomb rather than a square grid.
                  y = row * pitch_y + (pitch_y / 2 if col % 2 else 0.0)
                  # Keep the whole cell, ribs included, inside the border. The
                  # circumradius is the right reach to test with, not the
                  # apothem, for the same reason the pitch differs by axis.
                  cell_reach = cell_apothem * 2.0 / math.sqrt(3.0)
                  if math.hypot(x, y) <= INNER_APOTHEM_MM - cell_reach - LATTICE_RIB_MM:
                      holes.append((x, y))

          if holes:
              with BuildSketch(Plane.XY) as lattice:
                  with Locations(*holes):
                      RegularPolygon(cell_apothem, 6, major_radius=False)
              extrude(amount=PANEL_T_MM, mode=Mode.SUBTRACT)

      # Clamped so at least 2 mm of border survives on each side of the hole.
      # At the schema's minimum border width an unclamped 10 mm hole would leave
      # a 1.5 mm sliver that splits the first time the tile is hung.
      hang_d = min(HANG_D_MM, BORDER_W_MM - 4.0)
      if hang_d >= 3.0:
          with BuildSketch(Plane.XY) as hanger:
              with Locations((0, APOTHEM_MM - BORDER_W_MM / 2)):
                  Circle(hang_d / 2)
          extrude(amount=PANEL_T_MM + BORDER_H_MM, mode=Mode.SUBTRACT)

  result = tile.part
